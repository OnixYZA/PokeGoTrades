# Self-contained: this workspace does not rely on the app's root .gitignore.
node_modules/
dist/
# Holds the service role key and, once deployed, the Azure Functions storage connection string.
local.settings.json
.env
